import Terminal from "../../sections/Terminal";

/** Terminal window — the existing REPL, embedded (the Window supplies chrome). */
export default function TerminalApp() {
  return <Terminal embedded />;
}
